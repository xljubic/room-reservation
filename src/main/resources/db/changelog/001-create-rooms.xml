<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

    <changeSet id="001-create-rooms" author="sale">
        <createTable tableName="rooms">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="code" type="VARCHAR(30)">
                <constraints nullable="false"/>
            </column>

            <column name="capacity" type="INT">
                <constraints nullable="false"/>
            </column>

            <column name="building" type="VARCHAR(30)">
                <constraints nullable="false"/>
            </column>

            <column name="floor_level" type="INT">
                <constraints nullable="false"/>
            </column>

            <column name="room_type" type="VARCHAR(40)">
                <constraints nullable="false"/>
            </column>

            <column name="number_of_computers" type="INT">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addUniqueConstraint tableName="rooms"
                             columnNames="code"
                             constraintName="uq_rooms_code"/>
    </changeSet>

</databaseChangeLog>
